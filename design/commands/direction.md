---
description: Explore and define your project's visual direction through structured dialogue
argument-hint: "[amend | variant | fresh]"
---

# /design:direction

Explore and define your project's visual identity through structured dialogue. Generates a custom preset file that integrates with the design quality engine.

Load the [design-direction skill](../skills/design-direction/SKILL.md) and execute with `$ARGUMENTS`.
