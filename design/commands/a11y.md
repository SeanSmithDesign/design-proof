---
description: Run accessibility and visual design review (a11y layer only)
argument-hint: "[file-path | component-name]"
---

# /design:a11y

Load the [a11y layer](../skills/design-quality/layers/a11y.md) and run a standalone accessibility audit on `$ARGUMENTS`.

Respect **Strictness** and **Teaching** settings from the project's `CLAUDE.md` `## Design Quality` section (defaults: `standard`, `normal`).

Use the standalone output format defined in the a11y layer — the A11Y DESIGN REVIEW banner format with severity sections and summary score.

For a full composite audit (craft + aesthetic + a11y), use `/design:review` instead.
